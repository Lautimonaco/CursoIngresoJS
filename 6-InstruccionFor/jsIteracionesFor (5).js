function Mostrar() {

    var numero = prompt("Ingrese un numero:");
    numero = parseInt(numero);

    for (; num != 9;) {

        while (isNaN(num)) {
            numero = prompt("Ingrese un numero:");
            numero = parseInt(num);
        }

        if (num == 9) {
            break;
        }

    }
}
