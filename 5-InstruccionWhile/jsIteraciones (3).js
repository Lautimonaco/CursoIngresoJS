function mostrar() {

    var clave = prompt("ingrese el número clave.");

    while (!(clave == "utn750")) {
       
        clave == prompt("Error, ingrese la clave nuevamente");
        break;
    }

}//FIN DE LA FUNCIÓN
